<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ca.yorku.cmg.istardt</groupId>
    <artifactId>istar-unmarshal</artifactId>
    <version>0.2.1-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>dtx2X</name>
    <description>Java-based system for deserializing iStarDT-X XML models into a structured object model.</description>
    <url>https://github.com/cmg-yorku/dtx2X</url>

    <developers>
        <developer>
            <id>nina</id>
            <name>Nina Dang</name>
            <email>nina2022@my.yorku.ca</email>
        </developer>
        <developer>
            <id>liaskos</id>
            <name>Sotirios Liaskos</name>
            <email>liaskos@yorku.ca</email>
        </developer>
    </developers>

    <contributors>
        <contributor>
            <name>Some One</name>
            <email>some@one.ca</email>
        </contributor>
    </contributors>

    <organization>
      <name>Conceptual Modeling Group @ York University</name>
      <url>https://cmg.info.yorku.ca/</url>
    </organization>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.11.0</junit.version>
        <argLine></argLine>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.github.nina2dv</groupId>
            <artifactId>xml-validation</artifactId>
            <version>1.0.1-SNAPSHOT</version>
        </dependency>

        <!-- JUnit 5 dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Jackson XML -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-xml</artifactId>
            <version>2.15.2</version>
        </dependency>

        <!-- Jackson Core -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>2.15.2</version>
        </dependency>

        <!-- Jackson Databind -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.2</version>
        </dependency>

        <!-- Jackson Annotations -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>2.15.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>ca.yorku.cmg.istardt.translators.dtx2dtg.dtx2dtg</mainClass>
                </configuration>
            </plugin>

            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <!-- Maven Surefire Plugin for running tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <argLine>@{argLine}</argLine>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>3.21.0</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.doxia</groupId>
                <artifactId>doxia-module-markdown</artifactId>
                <version>2.0.0</version>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            
            
            
      		<plugin>
      			<groupId>org.apache.maven.plugins</groupId>
      			<artifactId>maven-antrun-plugin</artifactId>
      			<version>3.1.0</version>
      			<executions>
                    <execution>
                        <id>copy-after-tests</id>
                        <phase>test</phase>  <!-- or use post-integration-test -->
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <!--
                        <configuration>
                            <target>
                                <copy todir="../dtg2sim-new/examples/">
                                    <fileset dir="src/test/resources/dtx2dtg" includes="**/*.pl" excludes="**/*-Auth.*">
                                        <exclude name="**/DT-Golog.pl"/>
                                        <exclude name="**/DT-Golog-Iface.pl"/>
                                        <exclude name="**/DT-Golog-Ext.pl"/>
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                        -->
                    </execution>
	        		<execution>
	          			<id>publish-docs</id>
	          			<goals>
	            			<goal>run</goal>
	          			</goals>
	          			<configuration>
	            			<target>
	              				<echo message="Copying Maven site from target/site to docs/site"/>

	              				<delete dir="${basedir}/docs/site"/>
	              				<mkdir dir="${basedir}/docs/site"/>

	              				<copy todir="${basedir}/docs/site" overwrite="true">
	                					<fileset dir="${project.build.directory}/site"/>
	              				</copy>
	            			</target>
	          			</configuration>
	        		</execution>
      			</executions>
    		</plugin>
        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>3.6.1</version>
                <reportSets>
                    <reportSet>
                        <reports>
                            <report>summary</report>
                            <report>team</report>
                            <report>dependencies</report>
                            <report>dependency-info</report>
                            <report>dependency-management</report>
                            <report>plugin-management</report>
                            <report>plugins</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.1</version>
                <configuration>
                    <source>17</source>
                    <doclint>none</doclint>
                </configuration>
            </plugin>
        </plugins>
    </reporting>
</project>